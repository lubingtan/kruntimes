{{- if and .Values.dashboard.enabled .Values.dashboard.tls.certManager.enabled }}
{{- include "kruntimes.dashboard.validateTLS" . }}
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "kruntimes.dashboard.name" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "kruntimes.dashboard.labels" . | nindent 4 }}
spec:
  secretName: {{ include "kruntimes.dashboard.tlsSecretName" . }}
  dnsNames:
    - {{ include "kruntimes.dashboard.name" . }}
    - {{ include "kruntimes.dashboard.name" . }}.{{ .Release.Namespace }}
    - {{ include "kruntimes.dashboard.name" . }}.{{ .Release.Namespace }}.svc
    - {{ include "kruntimes.dashboard.name" . }}.{{ .Release.Namespace }}.svc.cluster.local
  issuerRef:
    name: {{ .Values.dashboard.tls.certManager.issuerRef.name }}
    kind: {{ .Values.dashboard.tls.certManager.issuerRef.kind }}
    group: {{ .Values.dashboard.tls.certManager.issuerRef.group }}
{{- end }}
