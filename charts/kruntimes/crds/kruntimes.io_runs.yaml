---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.3
  name: runs.kruntimes.io
spec:
  group: kruntimes.io
  names:
    kind: Run
    listKind: RunList
    plural: runs
    shortNames:
    - rn
    singular: run
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.assignedPod
      name: Assigned Pod
      type: string
    - jsonPath: .spec.runtime
      name: Runtime
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Run is the Schema for the runs API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RunSpec defines the desired state of Run.
            properties:
              affinity:
                description: Affinity constrains or prefers placement relative to
                  other active Runs.
                properties:
                  runAffinity:
                    description: RunAffinity requires or prefers sharing a topology
                      with matching active Runs.
                    properties:
                      preferredDuringSchedulingIgnoredDuringExecution:
                        description: PreferredDuringSchedulingIgnoredDuringExecution
                          defines soft placement preferences.
                        items:
                          description: WeightedRunAffinityTerm applies a weight to
                            a preferred Run affinity term.
                          properties:
                            runAffinityTerm:
                              description: RunAffinityTerm is the preferred placement
                                term.
                              properties:
                                labelSelector:
                                  description: LabelSelector selects active namespace-local
                                    Runs.
                                  properties:
                                    matchExpressions:
                                      description: matchExpressions is a list of label
                                        selector requirements. The requirements are
                                        ANDed.
                                      items:
                                        description: |-
                                          A label selector requirement is a selector that contains values, a key, and an operator that
                                          relates the key and values.
                                        properties:
                                          key:
                                            description: key is the label key that
                                              the selector applies to.
                                            type: string
                                          operator:
                                            description: |-
                                              operator represents a key's relationship to a set of values.
                                              Valid operators are In, NotIn, Exists and DoesNotExist.
                                            type: string
                                          values:
                                            description: |-
                                              values is an array of string values. If the operator is In or NotIn,
                                              the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                              the values array must be empty. This array is replaced during a strategic
                                              merge patch.
                                            items:
                                              type: string
                                            type: array
                                            x-kubernetes-list-type: atomic
                                        required:
                                        - key
                                        - operator
                                        type: object
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    matchLabels:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                        map is equivalent to an element of matchExpressions, whose key field is "key", the
                                        operator is "In", and the values array contains only "value". The requirements are ANDed.
                                      type: object
                                  type: object
                                  x-kubernetes-map-type: atomic
                                topologyKey:
                                  description: TopologyKey identifies the supported
                                    Runtime Pod topology.
                                  enum:
                                  - kruntimes.io/runtime-pod
                                  type: string
                              required:
                              - labelSelector
                              - topologyKey
                              type: object
                              x-kubernetes-validations:
                              - message: labelSelector must not be empty
                                rule: has(self.labelSelector) && ((has(self.labelSelector.matchLabels)
                                  && size(self.labelSelector.matchLabels) > 0) ||
                                  (has(self.labelSelector.matchExpressions) && size(self.labelSelector.matchExpressions)
                                  > 0))
                            weight:
                              description: Weight is the relative preference weight.
                              format: int32
                              maximum: 100
                              minimum: 1
                              type: integer
                          required:
                          - runAffinityTerm
                          - weight
                          type: object
                        maxItems: 32
                        type: array
                      requiredDuringSchedulingIgnoredDuringExecution:
                        description: RequiredDuringSchedulingIgnoredDuringExecution
                          defines hard placement constraints.
                        items:
                          description: RunAffinityTerm matches active namespace-local
                            Runs for one topology.
                          properties:
                            labelSelector:
                              description: LabelSelector selects active namespace-local
                                Runs.
                              properties:
                                matchExpressions:
                                  description: matchExpressions is a list of label
                                    selector requirements. The requirements are ANDed.
                                  items:
                                    description: |-
                                      A label selector requirement is a selector that contains values, a key, and an operator that
                                      relates the key and values.
                                    properties:
                                      key:
                                        description: key is the label key that the
                                          selector applies to.
                                        type: string
                                      operator:
                                        description: |-
                                          operator represents a key's relationship to a set of values.
                                          Valid operators are In, NotIn, Exists and DoesNotExist.
                                        type: string
                                      values:
                                        description: |-
                                          values is an array of string values. If the operator is In or NotIn,
                                          the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                          the values array must be empty. This array is replaced during a strategic
                                          merge patch.
                                        items:
                                          type: string
                                        type: array
                                        x-kubernetes-list-type: atomic
                                    required:
                                    - key
                                    - operator
                                    type: object
                                  type: array
                                  x-kubernetes-list-type: atomic
                                matchLabels:
                                  additionalProperties:
                                    type: string
                                  description: |-
                                    matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                    map is equivalent to an element of matchExpressions, whose key field is "key", the
                                    operator is "In", and the values array contains only "value". The requirements are ANDed.
                                  type: object
                              type: object
                              x-kubernetes-map-type: atomic
                            topologyKey:
                              description: TopologyKey identifies the supported Runtime
                                Pod topology.
                              enum:
                              - kruntimes.io/runtime-pod
                              type: string
                          required:
                          - labelSelector
                          - topologyKey
                          type: object
                          x-kubernetes-validations:
                          - message: labelSelector must not be empty
                            rule: has(self.labelSelector) && ((has(self.labelSelector.matchLabels)
                              && size(self.labelSelector.matchLabels) > 0) || (has(self.labelSelector.matchExpressions)
                              && size(self.labelSelector.matchExpressions) > 0))
                        maxItems: 32
                        type: array
                    type: object
                  runAntiAffinity:
                    description: RunAntiAffinity requires or prefers avoiding a topology
                      with matching active Runs.
                    properties:
                      preferredDuringSchedulingIgnoredDuringExecution:
                        description: PreferredDuringSchedulingIgnoredDuringExecution
                          defines soft placement preferences.
                        items:
                          description: WeightedRunAffinityTerm applies a weight to
                            a preferred Run affinity term.
                          properties:
                            runAffinityTerm:
                              description: RunAffinityTerm is the preferred placement
                                term.
                              properties:
                                labelSelector:
                                  description: LabelSelector selects active namespace-local
                                    Runs.
                                  properties:
                                    matchExpressions:
                                      description: matchExpressions is a list of label
                                        selector requirements. The requirements are
                                        ANDed.
                                      items:
                                        description: |-
                                          A label selector requirement is a selector that contains values, a key, and an operator that
                                          relates the key and values.
                                        properties:
                                          key:
                                            description: key is the label key that
                                              the selector applies to.
                                            type: string
                                          operator:
                                            description: |-
                                              operator represents a key's relationship to a set of values.
                                              Valid operators are In, NotIn, Exists and DoesNotExist.
                                            type: string
                                          values:
                                            description: |-
                                              values is an array of string values. If the operator is In or NotIn,
                                              the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                              the values array must be empty. This array is replaced during a strategic
                                              merge patch.
                                            items:
                                              type: string
                                            type: array
                                            x-kubernetes-list-type: atomic
                                        required:
                                        - key
                                        - operator
                                        type: object
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    matchLabels:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                        map is equivalent to an element of matchExpressions, whose key field is "key", the
                                        operator is "In", and the values array contains only "value". The requirements are ANDed.
                                      type: object
                                  type: object
                                  x-kubernetes-map-type: atomic
                                topologyKey:
                                  description: TopologyKey identifies the supported
                                    Runtime Pod topology.
                                  enum:
                                  - kruntimes.io/runtime-pod
                                  type: string
                              required:
                              - labelSelector
                              - topologyKey
                              type: object
                              x-kubernetes-validations:
                              - message: labelSelector must not be empty
                                rule: has(self.labelSelector) && ((has(self.labelSelector.matchLabels)
                                  && size(self.labelSelector.matchLabels) > 0) ||
                                  (has(self.labelSelector.matchExpressions) && size(self.labelSelector.matchExpressions)
                                  > 0))
                            weight:
                              description: Weight is the relative preference weight.
                              format: int32
                              maximum: 100
                              minimum: 1
                              type: integer
                          required:
                          - runAffinityTerm
                          - weight
                          type: object
                        maxItems: 32
                        type: array
                      requiredDuringSchedulingIgnoredDuringExecution:
                        description: RequiredDuringSchedulingIgnoredDuringExecution
                          defines hard placement constraints.
                        items:
                          description: RunAffinityTerm matches active namespace-local
                            Runs for one topology.
                          properties:
                            labelSelector:
                              description: LabelSelector selects active namespace-local
                                Runs.
                              properties:
                                matchExpressions:
                                  description: matchExpressions is a list of label
                                    selector requirements. The requirements are ANDed.
                                  items:
                                    description: |-
                                      A label selector requirement is a selector that contains values, a key, and an operator that
                                      relates the key and values.
                                    properties:
                                      key:
                                        description: key is the label key that the
                                          selector applies to.
                                        type: string
                                      operator:
                                        description: |-
                                          operator represents a key's relationship to a set of values.
                                          Valid operators are In, NotIn, Exists and DoesNotExist.
                                        type: string
                                      values:
                                        description: |-
                                          values is an array of string values. If the operator is In or NotIn,
                                          the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                          the values array must be empty. This array is replaced during a strategic
                                          merge patch.
                                        items:
                                          type: string
                                        type: array
                                        x-kubernetes-list-type: atomic
                                    required:
                                    - key
                                    - operator
                                    type: object
                                  type: array
                                  x-kubernetes-list-type: atomic
                                matchLabels:
                                  additionalProperties:
                                    type: string
                                  description: |-
                                    matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                    map is equivalent to an element of matchExpressions, whose key field is "key", the
                                    operator is "In", and the values array contains only "value". The requirements are ANDed.
                                  type: object
                              type: object
                              x-kubernetes-map-type: atomic
                            topologyKey:
                              description: TopologyKey identifies the supported Runtime
                                Pod topology.
                              enum:
                              - kruntimes.io/runtime-pod
                              type: string
                          required:
                          - labelSelector
                          - topologyKey
                          type: object
                          x-kubernetes-validations:
                          - message: labelSelector must not be empty
                            rule: has(self.labelSelector) && ((has(self.labelSelector.matchLabels)
                              && size(self.labelSelector.matchLabels) > 0) || (has(self.labelSelector.matchExpressions)
                              && size(self.labelSelector.matchExpressions) > 0))
                        maxItems: 32
                        type: array
                    type: object
                type: object
              artifactInputs:
                description: |-
                  ArtifactInputs are materialized from the configured ArtifactStore into the
                  Run working directory before execution.
                items:
                  description: |-
                    ArtifactInput materializes an artifact stored outside etcd into a Run working
                    directory before execution.
                  properties:
                    path:
                      description: |-
                        Path is the relative destination below the Run working directory. File
                        artifacts are written to this path; directory artifacts are extracted into
                        this directory.
                      maxLength: 4096
                      minLength: 1
                      type: string
                    ref:
                      description: |-
                        Ref identifies immutable artifact content readable by the Runtime's
                        configured ArtifactStore.
                      properties:
                        contentType:
                          description: ContentType is the detected media type.
                          maxLength: 255
                          type: string
                        createdAt:
                          description: CreatedAt records when the artifact was stored.
                          format: date-time
                          type: string
                        digest:
                          description: Digest is the content digest, including its
                            algorithm prefix.
                          maxLength: 256
                          type: string
                        driver:
                          description: Driver identifies the ArtifactStore implementation.
                          enum:
                          - filesystem
                          - s3
                          type: string
                        location:
                          description: Location contains driver-specific storage coordinates.
                          properties:
                            filesystem:
                              description: Filesystem identifies an artifact in a
                                filesystem store.
                              properties:
                                path:
                                  description: Path is relative to the artifact store
                                    root.
                                  maxLength: 4096
                                  minLength: 1
                                  type: string
                                volumeClaimName:
                                  description: VolumeClaimName identifies the PVC
                                    backing the filesystem store.
                                  maxLength: 253
                                  type: string
                              required:
                              - path
                              type: object
                            s3:
                              description: S3 identifies an artifact in an S3-compatible
                                object store.
                              properties:
                                bucket:
                                  description: Bucket is the object store bucket.
                                  maxLength: 255
                                  minLength: 1
                                  type: string
                                key:
                                  description: Key is the object key within the bucket.
                                  maxLength: 4096
                                  minLength: 1
                                  type: string
                              required:
                              - bucket
                              - key
                              type: object
                          type: object
                          x-kubernetes-validations:
                          - message: exactly one artifact location must be set
                            rule: has(self.filesystem) != has(self.s3)
                        name:
                          description: |-
                            Name is the logical artifact name exposed by the Run.
                            Artifact collection enforces the same 255-byte upper bound.
                          maxLength: 255
                          minLength: 1
                          type: string
                        sizeBytes:
                          description: SizeBytes is the stored artifact size.
                          format: int64
                          minimum: 0
                          type: integer
                        type:
                          description: Type describes how the artifact content is
                            represented.
                          enum:
                          - file
                          - directory
                          - archive
                          - blob
                          type: string
                      required:
                      - createdAt
                      - driver
                      - location
                      - name
                      - sizeBytes
                      - type
                      type: object
                      x-kubernetes-validations:
                      - message: artifact location must match driver
                        rule: (self.driver == 'filesystem' && has(self.location.filesystem))
                          || (self.driver == 's3' && has(self.location.s3))
                  required:
                  - path
                  - ref
                  type: object
                  x-kubernetes-validations:
                  - message: path must be a relative path without empty, '.' or '..'
                      segments
                    rule: '!self.path.startsWith(''/'') && !self.path.split(''/'').exists(segment,
                      size(segment) == 0 || segment == ''.'' || segment == ''..'')'
                maxItems: 32
                type: array
              env:
                description: Env is the list of environment variables to set for execution.
                items:
                  description: EnvVar represents an environment variable present in
                    a Container.
                  properties:
                    name:
                      description: |-
                        Name of the environment variable.
                        May consist of any printable ASCII characters except '='.
                      type: string
                    value:
                      description: |-
                        Variable references $(VAR_NAME) are expanded
                        using the previously defined environment variables in the container and
                        any service environment variables. If a variable cannot be resolved,
                        the reference in the input string will be unchanged. Double $$ are reduced
                        to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                        "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                        Escaped references will never be expanded, regardless of whether the variable
                        exists or not.
                        Defaults to "".
                      type: string
                    valueFrom:
                      description: Source for the environment variable's value. Cannot
                        be used if value is not empty.
                      properties:
                        configMapKeyRef:
                          description: Selects a key of a ConfigMap.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the ConfigMap or its key
                                must be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                        fieldRef:
                          description: |-
                            Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                            spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                          properties:
                            apiVersion:
                              description: Version of the schema the FieldPath is
                                written in terms of, defaults to "v1".
                              type: string
                            fieldPath:
                              description: Path of the field to select in the specified
                                API version.
                              type: string
                          required:
                          - fieldPath
                          type: object
                          x-kubernetes-map-type: atomic
                        fileKeyRef:
                          description: |-
                            FileKeyRef selects a key of the env file.
                            Requires the EnvFiles feature gate to be enabled.
                          properties:
                            key:
                              description: |-
                                The key within the env file. An invalid key will prevent the pod from starting.
                                The keys defined within a source may consist of any printable ASCII characters except '='.
                                During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                              type: string
                            optional:
                              default: false
                              description: |-
                                Specify whether the file or its key must be defined. If the file or key
                                does not exist, then the env var is not published.
                                If optional is set to true and the specified key does not exist,
                                the environment variable will not be set in the Pod's containers.

                                If optional is set to false and the specified key does not exist,
                                an error will be returned during Pod creation.
                              type: boolean
                            path:
                              description: |-
                                The path within the volume from which to select the file.
                                Must be relative and may not contain the '..' path or start with '..'.
                              type: string
                            volumeName:
                              description: The name of the volume mount containing
                                the env file.
                              type: string
                          required:
                          - key
                          - path
                          - volumeName
                          type: object
                          x-kubernetes-map-type: atomic
                        resourceFieldRef:
                          description: |-
                            Selects a resource of the container: only resources limits and requests
                            (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                          properties:
                            containerName:
                              description: 'Container name: required for volumes,
                                optional for env vars'
                              type: string
                            divisor:
                              anyOf:
                              - type: integer
                              - type: string
                              description: Specifies the output format of the exposed
                                resources, defaults to "1"
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            resource:
                              description: 'Required: resource to select'
                              type: string
                          required:
                          - resource
                          type: object
                          x-kubernetes-map-type: atomic
                        secretKeyRef:
                          description: Selects a key of a secret in the pod's namespace
                          properties:
                            key:
                              description: The key of the secret to select from.  Must
                                be a valid secret key.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the Secret or its key must
                                be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                      type: object
                  required:
                  - name
                  type: object
                maxItems: 256
                type: array
              mode:
                description: Mode contains execution-mode-specific configuration.
                properties:
                  function:
                    description: Function configures callable function execution.
                    properties:
                      handler:
                        description: |-
                          Handler is the module.function to call.
                          The runtime imports and calls the function instead of running a script.
                        maxLength: 512
                        type: string
                      idleTimeoutSeconds:
                        description: |-
                          IdleTimeoutSeconds is the duration after the last invocation before the
                          function reservation can be released. This field is reserved for the
                          function-mode lifecycle implementation.
                        format: int32
                        minimum: 1
                        type: integer
                    type: object
                  session:
                    description: Session configures a stateful agent sandbox session.
                    properties:
                      idleTimeoutSeconds:
                        description: |-
                          IdleTimeoutSeconds is the duration after the last accepted command or file
                          mutation before the session is closed.
                        format: int32
                        minimum: 1
                        type: integer
                      operationTimeout:
                        description: |-
                          OperationTimeout limits one command or file-mutation operation. runtimed
                          rejects values above its administrator-configured maximum.
                        type: string
                      queueSize:
                        description: |-
                          QueueSize limits queued command and file-mutation operations for this
                          session. runtimed also applies its administrator-configured global limit.
                        format: int32
                        maximum: 32
                        minimum: 1
                        type: integer
                    type: object
                  task:
                    description: Task configures one-shot process execution.
                    properties:
                      args:
                        description: |-
                          Args is the list of arguments passed to the runtime for non-inline sources.
                          It is ignored when Source.Inline is set.
                        items:
                          maxLength: 8192
                          type: string
                        maxItems: 256
                        type: array
                      entrypoint:
                        description: |-
                          Entrypoint is the relative script file to execute for non-inline sources.
                          It is ignored when Source.Inline is set.
                          Linux PATH_MAX is 4096 bytes.
                        maxLength: 4096
                        type: string
                    type: object
                type: object
                x-kubernetes-validations:
                - message: exactly one of task, function, or session must be set
                  rule: (has(self.task) && !has(self.function) && !has(self.session))
                    || (!has(self.task) && has(self.function) && !has(self.session))
                    || (!has(self.task) && !has(self.function) && has(self.session))
              resources:
                description: |-
                  Resources declares logical Runtime capacity reserved by this Run.
                  When omitted, the Run requests one unit of the built-in "runs" resource.
                properties:
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Requests declares logical Runtime resources held from assignment until the
                      Run reaches a terminal state or a function registration is released.
                      These are independent from the Kubernetes container resources on a Runtime
                      Pod template.
                    type: object
                type: object
              retryPolicy:
                description: RetryPolicy is the retry strategy for the Run. If nil,
                  no retries are attempted.
                properties:
                  backoff:
                    description: |-
                      Backoff is the initial backoff duration between retries.
                      The backoff doubles after each retry (exponential backoff with 2x multiplier),
                      capped at 60 seconds.
                    type: string
                  maxAttempts:
                    description: |-
                      MaxAttempts is the maximum number of execution attempts (including the initial attempt).
                      Default: 1 (no retries).
                    format: int32
                    maximum: 100
                    minimum: 1
                    type: integer
                  retryableReasons:
                    description: |-
                      RetryableReasons lists the failure reasons that are eligible for retry.
                      If empty, all reasons except "Cancelled" are retryable.
                    items:
                      maxLength: 128
                      type: string
                    maxItems: 32
                    type: array
                type: object
              runtime:
                description: |-
                  Runtime is the execution environment type (e.g., "python").
                  It maps to the "runtime" label on Runtime Pods.
                  Runtime names are propagated to Kubernetes label values.
                maxLength: 63
                minLength: 1
                pattern: ^([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9]$
                type: string
              source:
                description: Source specifies where the code to run comes from.
                properties:
                  commitSHA:
                    description: |-
                      CommitSHA is the specific commit to check out.
                      The limit also permits symbolic refs while bounding object growth.
                    maxLength: 256
                    type: string
                  inline:
                    description: |-
                      Inline is a standalone script. In task and session modes, runtimed writes
                      it to the default script file. In function mode, InlinePath identifies the
                      file to materialize.
                      Mutually exclusive with RepoURL.
                      256 KiB keeps simple scripts well below the Kubernetes object size limit.
                    maxLength: 262144
                    type: string
                  inlinePath:
                    description: |-
                      InlinePath is the relative file path below the prepared working directory
                      where runtimed materializes inline function source. It is only valid when
                      inline source is used by function mode.
                      Linux PATH_MAX is 4096 bytes.
                    maxLength: 4096
                    minLength: 1
                    type: string
                  repoURL:
                    description: |-
                      RepoURL is the Git repository URL to clone before execution.
                      2048 characters accommodates conventional HTTPS and SSH Git URLs.
                    maxLength: 2048
                    type: string
                type: object
                x-kubernetes-validations:
                - message: inline and repoURL are mutually exclusive
                  rule: '!(has(self.inline) && has(self.repoURL))'
                - message: commitSHA requires repoURL
                  rule: '!has(self.commitSHA) || has(self.repoURL)'
                - message: inlinePath must be a relative file path without empty,
                    '.' or '..' segments
                  rule: '!has(self.inlinePath) || (!self.inlinePath.startsWith(''/'')
                    && !self.inlinePath.split(''/'').exists(segment, size(segment)
                    == 0 || segment == ''.'' || segment == ''..''))'
              termination:
                description: |-
                  Termination requests an irreversible termination transition. Immediate is
                  valid for every Run mode; Drain is valid only for Session Runs and may be
                  escalated to Immediate.
                properties:
                  mode:
                    description: Mode selects immediate cancellation or Session-specific
                      graceful drain.
                    enum:
                    - Immediate
                    - Drain
                    type: string
                required:
                - mode
                type: object
              timeout:
                description: |-
                  Timeout is the maximum duration the run is allowed to run.
                  If not set, the run runs with no time limit.
                type: string
              ttlSecondsAfterFinished:
                description: |-
                  TTLSecondsAfterFinished limits the lifetime of a finished Run.
                  If set to a positive value, the controller deletes the Run after this many seconds
                  from status.completionTime. If unset or zero, the Run is retained.
                format: int32
                minimum: 0
                type: integer
              workspace:
                description: Workspace references a namespace-local PersistentWorkspace
                  used by this Run.
                properties:
                  apiGroup:
                    default: kruntimes.io/v1alpha1
                    description: APIGroup identifies the served workspace API group
                      and version.
                    enum:
                    - kruntimes.io/v1alpha1
                    type: string
                  kind:
                    default: PersistentWorkspace
                    description: Kind identifies the workspace provider kind.
                    enum:
                    - PersistentWorkspace
                    type: string
                  name:
                    description: Name is the PersistentWorkspace name in the Run namespace.
                    maxLength: 253
                    minLength: 1
                    pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                    type: string
                required:
                - name
                type: object
            required:
            - mode
            - runtime
            type: object
            x-kubernetes-validations:
            - message: mode.task.entrypoint must be a relative path that does not
                contain '..'
              rule: '!has(self.mode.task) || !has(self.mode.task.entrypoint) || (has(self.source)
                && has(self.source.inline)) || (!self.mode.task.entrypoint.startsWith(''/'')
                && !self.mode.task.entrypoint.split(''/'').exists(segment, segment
                == ''..''))'
            - message: source.inlinePath is only valid for function mode with inline
                source
              rule: '!has(self.source) || !has(self.source.inlinePath) || (has(self.mode.function)
                && has(self.source.inline))'
            - message: function mode inline source requires source.inlinePath
              rule: '!has(self.mode.function) || !has(self.source) || !has(self.source.inline)
                || has(self.source.inlinePath)'
            - message: session mode does not support spec.workspace
              rule: '!has(self.mode.session) || !has(self.workspace)'
            - message: termination.mode Drain is only valid for session mode
              rule: '!has(self.termination) || self.termination.mode != ''Drain''
                || has(self.mode.session)'
            - message: runtime, source, mode, artifactInputs, env, timeout, retryPolicy,
                and resources are immutable after Run creation
              rule: self.runtime == oldSelf.runtime && has(self.source) == has(oldSelf.source)
                && (!has(self.source) || self.source == oldSelf.source) && self.mode
                == oldSelf.mode && has(self.artifactInputs) == has(oldSelf.artifactInputs)
                && (!has(self.artifactInputs) || self.artifactInputs == oldSelf.artifactInputs)
                && has(self.env) == has(oldSelf.env) && (!has(self.env) || self.env
                == oldSelf.env) && has(self.timeout) == has(oldSelf.timeout) && (!has(self.timeout)
                || self.timeout == oldSelf.timeout) && has(self.retryPolicy) == has(oldSelf.retryPolicy)
                && (!has(self.retryPolicy) || self.retryPolicy == oldSelf.retryPolicy)
                && has(self.resources) == has(oldSelf.resources) && (!has(self.resources)
                || self.resources == oldSelf.resources)
            - message: workspace and affinity are immutable after Run creation
              rule: has(self.workspace) == has(oldSelf.workspace) && (!has(self.workspace)
                || self.workspace == oldSelf.workspace) && has(self.affinity) == has(oldSelf.affinity)
                && (!has(self.affinity) || self.affinity == oldSelf.affinity)
            - message: termination may not be removed, downgraded, or changed except
                from Drain to Immediate
              rule: '!has(oldSelf.termination) || (has(self.termination) && (self.termination
                == oldSelf.termination || (oldSelf.termination.mode == ''Drain'' &&
                self.termination.mode == ''Immediate'')))'
          status:
            description: RunStatus defines the observed state of Run.
            properties:
              artifactRefs:
                description: ArtifactRefs point to artifacts stored outside etcd.
                items:
                  description: ArtifactRef is compact metadata that points to artifact
                    content stored outside etcd.
                  properties:
                    contentType:
                      description: ContentType is the detected media type.
                      maxLength: 255
                      type: string
                    createdAt:
                      description: CreatedAt records when the artifact was stored.
                      format: date-time
                      type: string
                    digest:
                      description: Digest is the content digest, including its algorithm
                        prefix.
                      maxLength: 256
                      type: string
                    driver:
                      description: Driver identifies the ArtifactStore implementation.
                      enum:
                      - filesystem
                      - s3
                      type: string
                    location:
                      description: Location contains driver-specific storage coordinates.
                      properties:
                        filesystem:
                          description: Filesystem identifies an artifact in a filesystem
                            store.
                          properties:
                            path:
                              description: Path is relative to the artifact store
                                root.
                              maxLength: 4096
                              minLength: 1
                              type: string
                            volumeClaimName:
                              description: VolumeClaimName identifies the PVC backing
                                the filesystem store.
                              maxLength: 253
                              type: string
                          required:
                          - path
                          type: object
                        s3:
                          description: S3 identifies an artifact in an S3-compatible
                            object store.
                          properties:
                            bucket:
                              description: Bucket is the object store bucket.
                              maxLength: 255
                              minLength: 1
                              type: string
                            key:
                              description: Key is the object key within the bucket.
                              maxLength: 4096
                              minLength: 1
                              type: string
                          required:
                          - bucket
                          - key
                          type: object
                      type: object
                      x-kubernetes-validations:
                      - message: exactly one artifact location must be set
                        rule: has(self.filesystem) != has(self.s3)
                    name:
                      description: |-
                        Name is the logical artifact name exposed by the Run.
                        Artifact collection enforces the same 255-byte upper bound.
                      maxLength: 255
                      minLength: 1
                      type: string
                    sizeBytes:
                      description: SizeBytes is the stored artifact size.
                      format: int64
                      minimum: 0
                      type: integer
                    type:
                      description: Type describes how the artifact content is represented.
                      enum:
                      - file
                      - directory
                      - archive
                      - blob
                      type: string
                  required:
                  - createdAt
                  - driver
                  - location
                  - name
                  - sizeBytes
                  - type
                  type: object
                  x-kubernetes-validations:
                  - message: artifact location must match driver
                    rule: (self.driver == 'filesystem' && has(self.location.filesystem))
                      || (self.driver == 's3' && has(self.location.s3))
                maxItems: 32
                type: array
              artifactStore:
                description: |-
                  ArtifactStore is the immutable cleanup configuration captured before
                  artifacts are uploaded. It allows cleanup to continue if the Runtime is
                  later changed or deleted. Secret contents are never copied here.
                properties:
                  driver:
                    description: Driver identifies the configured artifact storage
                      backend.
                    enum:
                    - filesystem
                    - s3
                    type: string
                  filesystem:
                    description: Filesystem configures a PVC-backed filesystem artifact
                      store.
                    properties:
                      volumeClaimName:
                        description: VolumeClaimName is the PVC mounted into runtimed
                          for durable artifact storage.
                        minLength: 1
                        type: string
                    required:
                    - volumeClaimName
                    type: object
                  s3:
                    description: S3 configures an S3-compatible object artifact store.
                    properties:
                      bucket:
                        description: Bucket is the S3 bucket used to store artifacts.
                        minLength: 1
                        pattern: ^[^/]+$
                        type: string
                      credentialsSecretName:
                        description: |-
                          CredentialsSecretName names a Secret whose keys are exposed to runtimed
                          as environment variables for the AWS SDK credential chain.
                        maxLength: 253
                        minLength: 1
                        type: string
                      endpoint:
                        description: Endpoint configures a custom S3-compatible service
                          endpoint.
                        maxLength: 2048
                        type: string
                      forcePathStyle:
                        description: ForcePathStyle enables path-style S3 addressing.
                        type: boolean
                      prefix:
                        description: Prefix is prepended to generated artifact object
                          keys.
                        maxLength: 1024
                        type: string
                      region:
                        description: Region overrides the AWS SDK region.
                        maxLength: 128
                        type: string
                      uploadConcurrency:
                        description: UploadConcurrency overrides the number of concurrent
                          multipart upload workers.
                        format: int32
                        minimum: 1
                        type: integer
                      uploadPartSize:
                        description: UploadPartSize overrides the multipart upload
                          part size in bytes.
                        format: int64
                        minimum: 5242880
                        type: integer
                    required:
                    - bucket
                    type: object
                required:
                - driver
                type: object
                x-kubernetes-validations:
                - message: exactly one artifact store configuration matching driver
                    must be set
                  rule: (self.driver == 'filesystem' && has(self.filesystem) && !has(self.s3))
                    || (self.driver == 's3' && has(self.s3) && !has(self.filesystem))
              assignedPod:
                description: AssignedPod is the name of the Runtime Pod assigned by
                  the scheduler.
                type: string
              assignedPodUID:
                description: |-
                  AssignedPodUID is the UID of AssignedPod. It distinguishes Pod-name reuse
                  and is set and cleared together with AssignedPod.
                maxLength: 253
                type: string
              attempt:
                description: Attempt is the current execution attempt number (1-based).
                format: int32
                type: integer
              completionTime:
                description: CompletionTime is when the run finished (success or failure).
                format: date-time
                type: string
              conditions:
                description: Conditions represent the current state of the Run's lifecycle
                  conditions.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                maxItems: 16
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              endpoint:
                description: |-
                  Endpoint is the stable gateway endpoint for a ready function or session
                  Run. It is absent for one-shot task Runs.
                properties:
                  caBundle:
                    description: |-
                      CABundle is the PEM trust bundle for the endpoint when it is not rooted in
                      a client-configured trust store.
                    format: byte
                    maxLength: 16384
                    type: string
                  protocol:
                    description: Protocol identifies the endpoint protocol.
                    enum:
                    - HTTP
                    - HTTPS
                    type: string
                  url:
                    description: URL is the invoke endpoint URL. The path includes
                      the immutable Run UID.
                    maxLength: 2048
                    type: string
                required:
                - protocol
                - url
                type: object
              message:
                description: |-
                  Message is a human-readable status or error message.
                  Status messages are diagnostic summaries, not execution logs.
                maxLength: 4096
                type: string
              outputs:
                additionalProperties:
                  type: string
                description: |-
                  Outputs is the key-value pairs exposed by this Run (from $OUTPUTS file).
                  These limits mirror the runtimed output parser's per-key bounds.
                maxProperties: 64
                type: object
              phase:
                default: Pending
                description: Phase is the current lifecycle phase of the run.
                enum:
                - Pending
                - Scheduled
                - Running
                - Ready
                - Finalizing
                - Succeeded
                - Failed
                - Timeout
                - Cancelled
                type: string
              startTime:
                description: StartTime is when the run began executing.
                format: date-time
                type: string
            required:
            - phase
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
