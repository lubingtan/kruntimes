---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.3
  name: persistentworkspaces.kruntimes.io
spec:
  group: kruntimes.io
  names:
    kind: PersistentWorkspace
    listKind: PersistentWorkspaceList
    plural: persistentworkspaces
    shortNames:
    - pw
    singular: persistentworkspace
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.runtime
      name: Runtime
      type: string
    - jsonPath: .status.boundPod
      name: Bound Pod
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PersistentWorkspace is the Schema for the persistentworkspaces
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PersistentWorkspaceSpec defines the desired state of PersistentWorkspace.
            properties:
              cleanupPolicy:
                default: DeleteAfterTTL
                description: CleanupPolicy controls how workspace data is cleaned
                  up.
                enum:
                - DeleteAfterTTL
                - Retain
                type: string
              mode:
                default: RuntimePodLocal
                description: Mode controls how the workspace is bound.
                enum:
                - RuntimePodLocal
                type: string
              runtime:
                description: |-
                  Runtime is the Runtime whose workspace volume backs this workspace.
                  The initial RuntimePodLocal mode binds to one pod of this Runtime.
                maxLength: 63
                minLength: 1
                pattern: ^([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9]$
                type: string
              ttlSecondsAfterUnused:
                description: |-
                  TTLSecondsAfterUnused is the number of seconds to retain workspace data
                  after the workspace becomes unused. It is ignored when cleanupPolicy is Retain.
                format: int32
                minimum: 0
                type: integer
            required:
            - runtime
            type: object
          status:
            description: PersistentWorkspaceStatus defines the observed state of PersistentWorkspace.
            properties:
              boundPod:
                description: BoundPod is the Runtime Pod currently backing this workspace.
                type: string
              boundPodUID:
                description: |-
                  BoundPodUID fences BoundPod so a Pod recreated with the same name cannot
                  silently replace a RuntimePodLocal workspace.
                maxLength: 253
                type: string
              conditions:
                description: Conditions represent the current state of the workspace
                  lifecycle.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                maxItems: 16
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastUsedTime:
                description: LastUsedTime is the last time a Run used this workspace.
                format: date-time
                type: string
              path:
                description: Path is the runtime-local workspace path.
                type: string
              phase:
                default: Pending
                description: Phase is the current lifecycle phase.
                enum:
                - Pending
                - Bound
                - Lost
                - Released
                type: string
              runtime:
                description: Runtime is the observed Runtime backing this workspace.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
